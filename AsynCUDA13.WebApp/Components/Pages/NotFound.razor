@page "/not-found"
@inject LanguageService L

<div class="container mt-4 text-center">
    <RadzenIcon Icon="warning" Style="font-size: 4rem; color: #dc3545;" />
    <h3 class="mt-3">@L["NotFound"]</h3>
    <p class="text-muted">@L["PageNotFound"]</p>
    <RadzenButton Text="@L["Home"]" ButtonStyle="ButtonStyle.Primary" Click="NavigateToHome" />
</div>

@code {
    private void NavigateToHome()
    {
        // Navigation to home page
    }
}